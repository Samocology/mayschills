services:
  - type: web
    name: mayschills
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node server.mjs
    envVars:
      - key: NODE_ENV
        value: production
